use Foo\bar\baz;
namespace Test\Bob;
class Test extends TestA implements Bob, Bob2 {
    pub x: string;

    pub fn construct() {
        say_hi()
    }

    pub fn print(): void {
        print('${this.x}, world!');
    }

    pub stat fn write(value: string): Self {
        var self = new Self();

        return self;
    }
}

fn main() {
    var test = new Test();
    test.print();
}