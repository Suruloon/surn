namespace Test {
    // These variables are not public.
    // To export them, you must use `pub` keyword.
    var x = function_call(10, x);
    var apple = {
        test: 10
    };
    var test2 = this.is::crazy(foo(10), bob);
    var test = "This is a test string";
    var apple2;

    public var test3 = "This is a test string that is public";

    fn testMain() {
        var test = new Test();
        1 + 3;
        true + false;
        test.print();
    }
};

pub fn main() {
    // async is not supported yet.
    class Test {
        pub none: string;

        pub fn construct() {
            this.none = "This is a new initialized value";
        }

        pub fn print() {
            print("${this.none}");
        }
    }
}